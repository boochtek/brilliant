# Make sure we're using the latest Homebrew.
update

# Upgrade any already-installed formulas.
upgrade

# We need LLVM (3.4) for Ruby-LLVM, and we need the executables in $PATH.
install llvm --shared
link llvm --force

# Remove any outdated versions.
cleanup
